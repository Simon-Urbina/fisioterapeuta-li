# apps/

Carpeta contenedora de las dos aplicaciones con interfaz de usuario: el sitio
web (`web/`) y el bot de Telegram (`telegram-bot/`). No tiene código propio;
revisa el README de cada subcarpeta.
